checks = ["all"]

# Kullanılmayan semboller (gelecek API için tutulan helper'lar)
ignore "U1000" {
  files = [
    "api/http_server.go",            # geçici değişkenler
    "blockchain/transaction.go",     # encodeSig/decodeSig/pubKeyToECDSA
    "miner/assemble_block.go",       # assembleCoinbaseAndMetadata
  ]
  justification = "Kept for future API/back-compat"
}

# Printf yanlış kullanımı - UI mesajları (davranış değişimi istemiyoruz)
ignore "SA1006" {
  files = [
    "ui/send_ui.go",
  ]
  justification = "UI prints kept as-is"
}

# Eski/uyumluluk API'leri - Fyne/deprecated, RIPEMD-160 (Bitcoin uyumu)
ignore "SA1019" {
  files = [
    "miner/difficulty.go",  # theme.PrimaryColor
    "ui/settings_ui.go",    # theme.DarkTheme/LightTheme
    "utils/hash.go",        # ripemd160 for address compatibility
  ]
  justification = "Legacy compatibility kept"
}

# Gereksiz nil kontrolü - stil tercihi, davranış aynı
ignore "S1031" {
  files = [
    "blockchain/nft.go",
  ]
  justification = "Readable explicit nil-check kept"
}
