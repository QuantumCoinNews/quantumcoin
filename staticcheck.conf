# staticcheck.conf (TOML)

checks = ["all"]

# --- UNUSED / Geleceğe dönük helperlar ---
[[ignore]]
check = "U1000"                         # tek bir kural kodu veya regex
files = [
  "api/http_server.go",
  "blockchain/transaction.go",
  "miner/assemble_block.go",
]
reason = "Kept for future API/back-compat"

# --- UI Printf format uyarıları ---
[[ignore]]
check = "SA1006"
files = [
  "ui/send_ui.go",
]
reason = "UI prints kept as-is"

# --- Eski/uyumluluk API'leri ---
[[ignore]]
check = "SA1019"
files = [
  "miner/difficulty.go",
  "ui/settings_ui.go",
  "utils/hash.go",
]
reason = "Legacy compatibility kept"

# --- Stil tercihi: explicit nil-check ---
[[ignore]]
check = "S1031"
files = [
  "blockchain/nft.go",
]
reason = "Readable explicit nil-check kept"
