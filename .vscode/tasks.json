{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "HATA TARAMA: full",
      "type": "shell",
      "command": "go clean -cache -modcache && go fmt ./... && go vet ./... && go build ./...",
      "options": { "cwd": "${workspaceFolder}" },
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": ["$go"]
    }
  ]
}